# LLM Wiki 维护规则（Schema）

本仓库遵循 Karpathy 的 [LLM Wiki](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) 模式：**LLM 是程序员，Obsidian 是 IDE，wiki 是代码库**。本文件是给 LLM 代理（opencode / Claude Code / Codex 等）看的配置，定义 wiki 如何组织、如何维护。

## 三层架构

```
LLM Wiki/
├── AGENTS.md          # schema 层 —— 本文件，LLM 的维护规则（你可与此文件共同演化）
├── index.md           # 内容目录：wiki 里每一页的清单（按类别组织）
├── log.md             # 时间日志：append-only 的操作记录
├── raw/               # 原始来源层（不可变，LLM 只读不改）
│   ├── assets/        # 剪藏文章时下载的本地图片（Obsidian 附件目录）
│   └── <来源文件>     # 文章、论文、播客逐字稿、数据文件等
└── wiki/              # wiki 层（LLM 全权维护，人只读）
    ├── 首页.md        # 总览页：领域介绍 + 链接到核心页面
    ├── entities/      # 实体页：人物、组织、作品、角色、地点
    ├── concepts/      # 概念页：理论、方法、机制、术语
    ├── sources/       # 来源页：每个原始来源对应一篇总结
    └── synthesis/     # 综合页：对比、演化脉络、论点、交叉分析
```

**铁律：**
- **定位：精神指引与思维罗盘**。本仓库仅沉淀长青模型、底层原理、历史隐喻、决策框架与深刻洞察，作为拨乱反正的思考基座。
- **入库门槛**：**严格过滤时效性/临时性内容**（如特定版本的配置 Hack、软件临时教程、短效新闻）。不具备长期重读价值的内容一律不入库。
- `raw/` 下的原始文件**永不修改**。要记录变化（如补丁、勘误），写进 wiki 或 log，不要改源文件。
- wiki 层只由 LLM 写，人只在对话里给方向和反馈。
- 你的偏好、领域约定、踩过的坑，都应该沉淀进本文件（AGENTS.md），而不是只存在对话里。

## 页面规范

### 命名
- 文件名即页面名，使用清晰的名词短语：`Steve_Jobs.md`、`注意力机制.md`、`Transformer.md`。
- 页面标题（frontmatter 的 `title`）用人类可读名称，可含空格。
- 用 `[[页面名]]` 链接，不用绝对路径链接。链接到不存在的页面（红色链接）是允许的——它标记"待建页"。

### frontmatter（供 Dataview 使用）
```yaml
---
title: 页面标题
type: concept            # entity | concept | source | synthesis | index | log
tags:
  - 领域标签
created: 2026-08-12
updated: 2026-08-12
sources: 3               # 该页基于多少个原始来源（可选）
---
```

### 页面类型约定
| 类型          | 用途            | 结构建议                                       |
| ----------- | ------------- | ------------------------------------------ |
| `entity`    | 人物/组织/作品等持久实体 | 定义 → 关键事实 → 相关来源 → 相关页面                    |
| `concept`   | 理论/方法/机制      | 一句话定义 → 展开解释 → 相关来源 → 反例/边界                |
| `source`    | 单个原始来源的总结     | 来源信息（作者/日期/链接）→ 核心论点 → 要点摘录 → 关联到 wiki 其他页 |
| `synthesis` | 跨来源的对比/论点/演化  | 结论先行 → 证据 → 争议/矛盾 → 关联页                    |

### 矛盾处理
新来源与旧页面冲突时，**不要直接删除旧说法**。在新来源页里记录分歧，并在受影响的页面里并列标注两种说法及出处，最后给出你的判断（标注"根据现有证据"）。

## 核心工作流

### 1. Ingest（摄入）
用户把来源放进 `raw/` 并说"处理这个"。流程：
1. 读原始来源（若是长文，分段读；图片先读文字再单独看图）。
2. 与用户讨论要点，确认要强调什么。
3. 在 `wiki/sources/` 写一篇来源总结页。
4. 新建或更新相关的实体页、概念页、综合页（单个来源可能触及 10-15 个页面）。
5. 更新 `index.md`（加新页、更新摘要）。
6. 在 `log.md` 追加一条 ingest 记录。
7. 每次改动都要更新对应页面的 `updated` 字段。

### 2. Query（问答）
用户提问时：
1. 先读 `index.md`，定位相关页面，再下钻阅读。
2. 综合答案并给出引用（`[[页面名]]` 或 `raw/` 里的原文出处）。
3. **值得沉淀的答案要归档**：对比表、分析、新发现 → 写成 `wiki/synthesis/` 下的新页，更新 index 和 log。好的答案不该消失在对话里。

### 3. Lint（体检）
用户说"检查 wiki"时：
- 找页面间矛盾、被新来源取代的过时说法、无入链的孤儿页、被提及但缺页的概念、缺失的交叉引用。
- 找出可以补的数据缺口，建议新的调研问题和新来源。
- 把结果整理成一份报告（可作为 synthesis 页），更新 log。

## 记录文件规则

- **index.md**：每页一行 `- [[页面名]] —— 一句话摘要`，按类别分组（实体/概念/来源/综合）。每次 ingest/归档都要更新。
- **log.md**：append-only，每条以 `## [YYYY-MM-DD] 操作类型 | 标题` 开头，例如：
  `## [2026-08-12] ingest | 某篇文章`
  类型：`ingest` / `query` / `lint` / `archive` / `setup`。
  日志保持可被 grep 解析：`grep "^## \[" log.md | tail -5`。

## 工具与环境

- 网页剪藏用 Obsidian Web Clipper，图片用热键（Ctrl+Shift+D）下载到 `raw/assets/`。
- 需要搜索 wiki 时，优先读 index，必要时用 `qmd`（若已安装）或自己写小脚本。
- 生成幻灯片用 Marp（Obsidian 插件）；图表用 matplotlib 或 Mermaid。
- 对话一律使用简体中文（除非用户明确要求其他语言）。
